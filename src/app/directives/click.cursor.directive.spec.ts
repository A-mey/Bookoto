import { ClickCursorDirective } from './click.cursor.directive';

describe('ClickCursorDirectiveTsDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickCursorDirective();
    expect(directive).toBeTruthy();
  });
});
